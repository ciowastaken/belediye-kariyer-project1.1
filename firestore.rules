rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Sadece oturum açmış kullanıcılar tüm verilere erişebilir
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}